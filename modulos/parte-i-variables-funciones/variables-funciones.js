// Variables
//int a = 0; -> no existe

let numero;
let letra = "A";
numero = 12;
const IVA = 21;

// FUNCIONES
// Declaradas
// Expresadas
// arrow Functions o funciones flecha

function sumar(a, b) {
  return a + b;
}
sumar(3, 1);

const total = function (a, b) {
  return a + b;
};

total(2, 2);

const calcularSuma = (a, b) => a + b;

calcularSuma(2, 3);
